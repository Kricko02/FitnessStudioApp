<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:FitnessStudioApp.ViewModels"
             x:Class="FitnessStudioApp.Views.ProfilePage"
             x:DataType="viewmodel:ProfileViewModel"
             Title="ProfilePage">

    <VerticalStackLayout>
        <Label 
            Text="{Binding Username}"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />

        <ListView ItemsSource="{Binding UserWorkouts}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="basidja" /> <!--Tu bi moglo bit zdaj nprimer {Binding Name} ker mam UserWorkout je List<WorkoutResoponsov> ko me property name id...... in tak bi zdej lahk prikazal vse workoute ampak binding ne dela se pa naložijo v list in se zdja za vsak workout izpiše basdshifsdbgdsf....-->
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </VerticalStackLayout>
</ContentPage>